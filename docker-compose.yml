services:
  frontend:
    image: ghcr.io/goranjovic55/nop-frontend:latest
    pull_policy: always
    ports:
      - "12000:3000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - REACT_APP_API_URL=http://localhost:8000
      - REACT_APP_WS_URL=ws://localhost:8000
    depends_on:
      - backend
    networks:
      - nop-internal
    restart: unless-stopped

  backend:
    image: ghcr.io/goranjovic55/nop-backend:latest
    pull_policy: always
    network_mode: host
    environment:
      - DATABASE_URL=postgresql+asyncpg://nop:nop_password@172.28.0.10:5432/nop
      - REDIS_URL=redis://172.28.0.11:6379/0
      - GUACD_HOST=127.0.0.1
      - GUACD_PORT=24822
      - SECRET_KEY=your-secret-key-change-this
      - ADMIN_PASSWORD=admin123
      - VNC_HOST=172.21.0.51
      - RDP_HOST=172.21.0.50
    depends_on:
      - postgres
      - redis
      - guacd
    volumes:
      - ./volumes/evidence:/app/evidence
      - ./volumes/logs:/app/logs
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
    privileged: true
    cap_add:
      - NET_RAW
      - NET_ADMIN

  postgres:
    image: postgres:15-alpine
    container_name: nop-postgres
    environment:
      - POSTGRES_USER=nop
      - POSTGRES_PASSWORD=nop_password
      - POSTGRES_DB=nop
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      nop-internal:
        ipv4_address: 172.28.0.10
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: nop-redis
    networks:
      nop-internal:
        ipv4_address: 172.28.0.11
    restart: unless-stopped

  guacd:
    image: ghcr.io/goranjovic55/nop-guacd:latest
    container_name: nop-guacd
    network_mode: host
    environment:
      - GUACD_LOG_LEVEL=info
    entrypoint: ["/usr/local/sbin/guacd", "-b", "127.0.0.1", "-l", "24822", "-f"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 24822 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s

networks:
  nop-internal:
    driver: bridge
    ipam:
      config:
        - subnet: 172.28.0.0/16
  test-network:
    driver: bridge

volumes:
  postgres_data:
