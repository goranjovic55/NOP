---
session:
  id: "2026-01-29_topology-fixes"
  complexity: medium

skills:
  loaded: [frontend-react, backend-api, debugging, testing]

files:
  modified:
    - {path: "backend/app/services/SnifferService.py", domain: backend}
    - {path: "frontend/src/pages/Topology.tsx", domain: frontend}
    - {path: "e2e/tests/topology-fixes.spec.ts", domain: testing}

root_causes:
  - problem: "LLDP/CDP hostnames showing as b'...' byte strings"
    solution: "Added _decode_scapy_field() helper to decode bytes to UTF-8 strings"
  - problem: "New nodes stack at origin (0,0) on refresh instead of spreading"
    solution: "Detect nodes not in nodePositionsRef and assign random positions around graph center"
  - problem: "L2/L3 data not appearing together - race condition"
    solution: "Moved L2 fetch inside fetchData() to ensure synchronized arrival with L3 data"
  - problem: "Topology doesn't load until switching auto/manual modes"
    solution: "Initial useEffect had empty deps [], missed token availability - added [token] dependency"

gotchas:
  - "TypeScript closure null narrowing: Use local binding (l2DataSnapshot = currentL2Data) for type safety inside callbacks"
  - "React useEffect with auth token: Always include token in deps for initial data fetch"
---

# Session: Topology Fixes (4 Issues)

## Summary
Fixed 4 topology visualization issues: byte string decoding in LLDP/CDP parsing, new node positioning on refresh, L2/L3 data synchronization, and initial page load in auto mode.

## Tasks
- ✓ Fix 1: Byte string decoding in SnifferService.py
- ✓ Fix 2: New node auto-repositioning in Topology.tsx
- ✓ Fix 3: L2/L3 data fetch synchronization
- ✓ Fix 4: MAC-based asset merging (existing code sufficient)
- ✓ Fix 5: Initial page load with token dependency
- ✓ Created E2E tests for validation
- ✓ Rebuilt and verified all fixes

## Changes

### Backend: SnifferService.py
Added `_decode_scapy_field()` helper at line 35:
```python
def _decode_scapy_field(field: any) -> str:
    if isinstance(field, bytes):
        return field.decode('utf-8', errors='replace')
    elif isinstance(field, str):
        return field
    return str(field) if field else ''
```
Used in LLDP parsing (~L675) and CDP parsing (~L735).

### Frontend: Topology.tsx
1. **L2 fetch inside fetchData()** (~L977): Changed from separate useEffect to inline fetch for synchronization
2. **New node detection** (~L1710): Detect nodes missing from nodePositionsRef, assign positions in ring around center
3. **Token dependency** (~L1787): Changed `useEffect` deps from `[]` to `[token]` for proper initial load
4. **Type safety**: Added `l2DataSnapshot` local binding to fix TypeScript null narrowing in closures

### Testing: topology-fixes.spec.ts
Created comprehensive E2E test suite with 5 test groups covering all fixes.

## Verification
- Python syntax: OK
- TypeScript build: OK (after fixing null narrowing)
- Docker rebuild: Success
- User testing: All issues resolved
